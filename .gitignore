# .NET
bin/
obj/
*.user

# Node / Vite
node_modules/
dist/
web/.env.local

# IDE
.vs/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# DB dumps from scripts/backup.sh — never commit real data
backups/

# Parallel agent worktrees (each is its own git checkout, not repo content)
.claude/worktrees/
