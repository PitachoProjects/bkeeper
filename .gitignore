# .NET
bin/
obj/
*.user

# Node / Vite
node_modules/
dist/
web/.env.local

# Local secrets (real DB credentials, etc.) — never commit
.env

# IDE
.vs/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# DB dumps from scripts/backup.sh — never commit real data
backups/
